<!DOCTYPE html>
<html lang="es">

    <%- include('./partials/head',{title: 'Mundo Pets'}) %> 

<body class="productos">
    <%- include('./partials/header') %> 
    <main> 
        
       
        <h1> Productos para <%= categorias.nombre %></h1>
        <% if (categorias.nombre === 'Gato') { %>
            <%- include('./partials/bannerGatos') %> 
          <% } else { %>
            <%- include('./partials/bannerPerros') %> 
            <% } %>
       
       
       

        <section class="carrouselProductos">
            <div class="glider-contain">
                <div class="glider">
                    <% categorias.productos.forEach(producto => { %>
                        <%- include('./partials/cards',{
                            id:producto.id,
                            imagenes:producto.imagen,
                            nombre:producto.titulo,
                            descuento:producto.descuento,
                            precio:producto.precio
                        }) %> 
                    <% }) %>
                </div>             
                <button aria-label="Previous" class="glider-prev"><i class="fas fa-chevron-left"></i></button>
                <button aria-label="Next" class="glider-next"><i class="fas fa-chevron-right"></i></button>
                <div role="tablist" class="dots"></div>
              </div>
        </section>
    </main>
    <%- include('./partials/footer') %> 
</body>
</html>
       